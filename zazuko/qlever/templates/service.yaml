apiVersion: v1
kind: Service
metadata:
  name: {{ include "qlever.fullname" . }}
  labels:
    {{- include "qlever.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qlever.selectorLabels" . | nindent 4 }}
