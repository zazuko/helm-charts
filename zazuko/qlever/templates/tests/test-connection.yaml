apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qlever.fullname" . }}-test-connection"
  labels:
    {{- include "qlever.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qlever.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
